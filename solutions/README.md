# Solutions

Annotated reference implementations — **read these after you've actually
tried**, not before.

The comments explain *why* the approach works and what the alternative was, not
just what the code does. Where there's a faster variant (bucket sort instead of
a heap, counting instead of sorting), it's noted inline.

| Module | File |
|---|---|
| 00 — Foundations | [`m00_foundations/warmups.py`](m00_foundations/warmups.py) |
| 01 — Arrays & Hashing | [`m01_arrays_hashing/arrays_hashing.py`](m01_arrays_hashing/arrays_hashing.py) |

Reading a solution is not knowing it. Re-solve it from a blank file two days
later — that's what turns it into something that survives a gap.

[← back to the repo](../README.md)
